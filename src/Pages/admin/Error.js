import React from 'react';

const Error = () => {
    return (
        <div style={{fontSize: '2.5rem', display: 'grid', placeItems: 'center', height: '100vh'}}>
            Page non trouvée
        </div>
    );
};

export default Error;